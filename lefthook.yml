pre-commit:
  commands:
    prettier:
      run: npx prettier --write {staged_files}
      stage_fixed: true
    biome:
      run: npx @biomejs/biome lint --apply {staged_files}
      stage_fixed: true
