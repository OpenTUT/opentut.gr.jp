pre-commit:
  commands:
    prettier:
      glob: "*.astro"
      run: npx prettier --write {staged_files}
      stage_fixed: true
    biome:
      glob: "*.{js,ts,cjs,mjs,cts,mts,jsx,tsx,json,jsonc,astro}"
      run: npx @biomejs/biome check --apply {staged_files}
      stage_fixed: true
